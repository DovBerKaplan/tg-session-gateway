fastapi>=0.141.1
uvicorn[standard]>=0.29
pyrofork>=2.3.69  # the maintained fork consumers run; plain pyrogram 2.0.106 flattens <blockquote expandable>
aiosqlite>=0.20
cryptography>=42.0
pydantic>=2.6
# tgcrypto: optional C-accelerated crypto (install manually if build tools available)
# pip install tgcrypto
