fastapi>=0.110
uvicorn[standard]>=0.29
pyrogram>=2.0
aiosqlite>=0.20
cryptography>=42.0
pydantic>=2.6
# tgcrypto is a performance enhancement (C extension, optional)
# Install separately if build tools are available:
#   pip install tgcrypto
