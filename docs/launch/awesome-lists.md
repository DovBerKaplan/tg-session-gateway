# Awesome-list PRs (small, honest one-liners)

## goq/telegram-list (or the maintainer's preferred format)
- [**tg-session-gateway**](https://github.com/DovBerKaplan/tg-session-gateway) — Keep Telegram bot sessions alive across Docker deployments: session ownership, durable update queue, FAQ rate buckets; drop-in Bot API gateway + MTProto sidecar.

## Python/infra lists (awesome-python "Bot API" adjacent, awesome-selfhosted)
- tg-session-gateway — Long-lived Telegram session gateway (Bot API drop-in + MTProto sidecar) with rate limiting and crash-safe update queues. Python/Docker, MIT.

Rules for these PRs: one line, no adjectives that aren't test-linked,
open issue first if the list requires discussion.
